import type { Logger } from "pino";
import { DataStreamRouter, MessageHandlerRegistry } from "../DataStreamRouter";
export declare class _MessageRouter {
    readonly messageHandlers: MessageHandlerRegistry;
    readonly dataStreamRouter: DataStreamRouter;
    private readonly logger;
    constructor(logger: Logger);
    handleRawText(raw: string): boolean;
    destroy(): void;
}
//# sourceMappingURL=_MessageRouter.d.ts.map