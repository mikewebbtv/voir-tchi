import type { Logger } from "pino";
import { Transport } from "../../transport/Transport";
export interface _ConnectionManagerDeps {
    transport: Transport;
    logger: Logger;
    autoReconnect: boolean;
    maxReconnectAttempts: number;
    reconnectDelay: number;
    onTransportReady: () => void;
    onTextMessage: (raw: string) => void;
    onBinaryMessage: (data: ArrayBuffer) => void;
    onClose: (info: {
        code: number;
        reason: string;
        permanent: boolean;
    }) => void;
    onError: (error: Error) => void;
}
export declare class _ConnectionManager {
    private readonly deps;
    private connected;
    private parked;
    private explicitDisconnect;
    private reconnectAttempts;
    private reconnectTimer;
    private pingInterval;
    private parkedTimer;
    constructor(deps: _ConnectionManagerDeps);
    get isConnected(): boolean;
    get isParked(): boolean;
    connect(): Promise<void>;
    disconnect(): void;
    markConnected(): void;
    park(timeoutMs: number, onTimeout: () => void): void;
    destroy(): void;
    private attachTransportHandlers;
    private scheduleReconnect;
    private startPingInterval;
    private stopPingInterval;
    private stopReconnectTimer;
    private stopParkedTimer;
}
//# sourceMappingURL=_ConnectionManager.d.ts.map