import { EventEmitter } from "events";
import type { AudioOutputStream } from "../managers/SpeakerManager";
export declare class _V2AudioStreamShim extends EventEmitter {
    private readonly stream;
    readonly streamId: string;
    constructor(stream: AudioOutputStream);
    get state(): string;
    write(chunk: Uint8Array): void;
    end(): Promise<void>;
    flush(): Promise<void>;
}
//# sourceMappingURL=_V2AudioStreamShim.d.ts.map